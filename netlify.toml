[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Set all serverless functions to the maximum allowed timeout
[functions]
  timeout = 26

[[plugins]]
  package = "@netlify/plugin-nextjs"
